# estoket_pro

TODO: Add constraint to check also whether a user belongs to a user_type before granting access to a branch. 
For instance:
    @login_required && if user_type == 'freelancer'

james@doe.com
james12345